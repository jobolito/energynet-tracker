---
import Base from '../layouts/Base.astro';
import Map from '../components/Map.astro';
import { loadProjects } from '../lib/data';
const projects = await loadProjects();
---
<Base title="Map" bare={true}>
  <Map projects={projects} mode="embed" />
</Base>
